[pytest]
testpaths = tests
# `auto` so async tests and async fixtures need no per-test marker. The existing
# engine/psychometrics suites are synchronous and unaffected.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning:passlib.*
